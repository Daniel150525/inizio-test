# Inizio Test

Jednoduchá webová aplikace ve Flasku, která:

- Zobrazuje HTML formulář pro zadání klíčového slova
- Po odeslání generuje JSON se simulovanými výsledky vyhledávání
- Obsahuje unit testy pro ověření správnosti výstupu
- Lze spustit lokálně nebo přes Docker
